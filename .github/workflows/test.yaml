name: test of github actions

on:
    push:
        branch: main

jobs:
    test:
        name: test of gha
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repo
              uses: actions/checkout@v3

            - name: Print text
              run: |
                python test.py